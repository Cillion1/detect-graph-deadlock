# detect-graph-deadlock

C++ program that determines whether a graph contains a deadlock or not by determing if said graph has a cycle.

Compile with g++

```
g++ -o deadlock deadlock.cpp
```

Run by

```
./deadlock < basic.txt
```
